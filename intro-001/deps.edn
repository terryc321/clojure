
;; with clojure no need to have nrepl as dependency this is handled by emacs + cider
;; cider-jack-in-clj

{;; project paths
 :paths ["src"]

 :deps
 {clojure.java-time/clojure.java-time {:mvn/version "1.4.3"}
  clj-antlr/clj-antlr {:mvn/version "0.2.14"}}}

;; if we want to interrupt long run process - requires java 21 + 
;; tools.dep
;; :aliases {:dev
;;           {:jvm-opts ["-Djdk.attach.allowAttachSelf"]
;;            ...}}
;;{:jvm-opts ["-Djdk.attach.allowAttachSelf"]



