(ns playground.playground-test
  (:require [clojure.test :refer [deftest is testing]]
            [playground.playground :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
