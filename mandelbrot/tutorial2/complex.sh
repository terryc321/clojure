#!/bin/bash

# just ignore this file it does not work and is not required using deps.edn

# install clojars complex number library - complex 0.1.12

# apparently none of this is needed it should all be taken of by using deps.edn
# and just starting clojure
# clj


# mvn -e -X install:install-file \
#   -Dfile=complex/complex-0.1.12.jar \
#   -DgroupId=complex \
#   -DartifactId=complex \
#   -Dversion=0.1.12 \
#   -Dpackaging=jar \
#   -DlocalRepositoryPath=/home/terry/.m2/repository   
